interface StepsItemsProps {
  heading: string;
  paragraph: string;
  image: string;
}

const StepsItems = ({ heading, paragraph, image }: StepsItemsProps) => {
  return <div>StepsItems</div>;
};

export default StepsItems;
